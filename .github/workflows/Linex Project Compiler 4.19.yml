name: Linex Project Compiler 4.19 Optimized Pro

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Nama Hasil Build'
        default: '4.19_kernel_beryllium_fa_ska'
      kernel-url:
        description: 'URL Repo Kernel'
        default: 'https://github.com/fa-ska/4.19_android_kernel_xiaomi_sdm845'
      kernel-branch:
        description: 'Branch'
        default: 'rebase'
      base-defconfig:
        description: 'Base Defconfig'
        default: 'sdm845-perf_defconfig'
      device-defconfig:
        description: 'Device Config'
        default: 'vendor/xiaomi/beryllium.config'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Environment Setup
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            bc bison build-essential curl flex git git-core gnupg gperf \
            libssl-dev libxml2-utils lzop make zip zstd python3 ca-certificates \
            clang lld llvm device-tree-compiler libelf-dev wget libdebuginfod-dev \
            rsync

      - name: Toolchain Setup (Proton Clang)
        run: |
          mkdir -p $HOME/proton-clang
          # Menggunakan Proton Clang terbaru (sangat stabil untuk 4.19)
          curl -LSs "https://github.com/kdrag0n/proton-clang/archive/refs/heads/master.tar.gz" | tar -C $HOME/proton-clang --strip-components=1 -xz

      - name: Source, Patching & Compilation
        run: |
          # 1. Ambil Source
          git clone --recursive --depth=1 ${{ inputs.kernel-url }} -b ${{ inputs.kernel-branch }} kernel
          cd kernel
          
          # 2. Hard-Fix Techpack & Header
          find . -name "Kconfig" -exec sed -i '/techpack/d' {} +
          find . -name "Makefile" -exec sed -i '/techpack/d' {} +
          sed -i 's/^YYLTYPE yylloc;/extern YYLTYPE yylloc;/g' scripts/dtc/dtc-lexer.lex.c_shipped || true
          
          # 3. Patching KernelSU
          curl -LSs "https://raw.githubusercontent.com/rifsxd/KernelSU-Next/next/kernel/setup.sh" | bash -s next
          
          # 4. Setup Environment
          export PATH="$HOME/proton-clang/bin:$PATH"
          export ARCH=arm64
          export KBUILD_BUILD_USER="fa_s_ka"
          export KBUILD_BUILD_HOST="linext"
          
          # Proton Clang tidak butuh GCC terpisah karena sudah self-contained
          MAKE_PARAMS=(
            ARCH=arm64
            O=out
            CC=clang
            CROSS_COMPILE=aarch64-linux-gnu-
            CROSS_COMPILE_ARM32=arm-linux-gnueabi-
            LD=ld.lld
            AR=llvm-ar
            NM=llvm-nm
            OBJCOPY=llvm-objcopy
            OBJDUMP=llvm-objdump
            STRIP=llvm-strip
          )

          mkdir -p out
          
          # 5. Create Config
          ./scripts/kconfig/merge_config.sh -O out \
            arch/arm64/configs/${{ inputs.base-defconfig }} \
            arch/arm64/configs/${{ inputs.device-defconfig }}

          # 6. Build Process
          make "${MAKE_PARAMS[@]}" oldconfig
          make "${MAKE_PARAMS[@]}" prepare -j$(nproc --all)
          make "${MAKE_PARAMS[@]}" scripts -j$(nproc --all)
          
          # Kompilasi Utama
          make -j$(nproc --all) "${MAKE_PARAMS[@]}" \
            KCFLAGS="-w -fpermissive -fno-stack-protector"

      - name: Packing
        run: |
          cd $GITHUB_WORKSPACE
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3 AnyKernel3
          # Cari Image yang berhasil dibuat
          for img in kernel/out/arch/arm64/boot/Image.gz-dtb kernel/out/arch/arm64/boot/Image.gz kernel/out/arch/arm64/boot/Image; do
            if [ -f "$img" ]; then
              cp "$img" AnyKernel3/
              break
            fi
          done
          [ -f kernel/out/arch/arm64/boot/dtbo.img ] && cp kernel/out/arch/arm64/boot/dtbo.img AnyKernel3/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.name }}
          path: AnyKernel3/*
